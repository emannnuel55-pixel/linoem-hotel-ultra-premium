import express from 'express';import path from 'node:path';import{fileURLToPath}from'node:url';const app=express(),root=path.dirname(fileURLToPath(import.meta.url));app.disable('x-powered-by');app.use(express.static(path.join(root,'public')));app.get('/config.js',(_,r)=>r.type('js').send(`window.LINOEM={API_URL:${JSON.stringify(process.env.API_URL||'http://localhost:4001')}}`));app.use((_,r)=>r.sendFile(path.join(root,'public/index.html')));app.listen(process.env.PORT||3001,()=>console.log('Panel empleados listo'));
