# Database Configuration
DATABASE_URL="postgresql://postgres:password@localhost:5432/hotel_db?schema=public"

# JWT Auth Secret
JWT_SECRET="super_secret_hotel_key_2026_ultra_premium"
JWT_EXPIRES_IN="1d"

# Server Port
PORT=8080

# CORS Origins
CORS_ORIGIN="*"

# Email / OTP Settings (Mock for dev, configure for prod)
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="user@example.com"
SMTP_PASS="password"
SMTP_FROM="Hotel Premium <no-reply@hotelpremium.com>"

# Stripe Checkout (Sandbox / Test Mode)
STRIPE_SECRET_KEY="sk_test_..."
