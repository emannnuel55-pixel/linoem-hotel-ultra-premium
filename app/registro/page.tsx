"use client";import {useState} from "react";export default function Page(){const[m,setM]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/auth/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(f))}),j=await r.json();setM(r.ok?"Cuenta creada. Revisa tu correo para verificarla.":j.error)}return <main className="auth"><section className="auth-art"><div className="brand">LINOEM HOTEL</div><h1>Tu experiencia<br/><span className="gold">empieza hoy.</span></h1></section><section className="auth-card"><form className="form" onSubmit={submit}><h2>Crear cuenta</h2><p>Regístrate como huésped de forma segura.</p>{m&&<div className="msg">{m}</div>}<label>Nombre completo</label><input name="name" required minLength={2}/><label>Correo personal</label><input name="email" type="email" required/><label>Contraseña segura</label><input name="password" type="password" required minLength={10}/><p>10 caracteres, mayúscula, minúscula y número.</p><button className="btn">Registrarme</button><a className="btn google" href="/api/auth/google">Registrarme con Google</a><div className="links"><a href="/login">Ya tengo cuenta</a></div></form></section></main>}
