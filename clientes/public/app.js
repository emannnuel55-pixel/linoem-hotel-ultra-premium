const A=window.LINOEM.API_URL; const demo=[{id:'1',name:'Suite Dorada Centro',type:'Suite',city:'Ciudad Juárez',people:2,price:1850,icon:'✦'},{id:'2',name:'Departamento Ejecutivo',type:'Departamento',city:'Chihuahua',people:4,price:2390,icon:'⌂'},{id:'3',name:'Casa Terraza Premium',type:'Casa',city:'Ciudad Juárez',people:6,price:3200,icon:'◇'}];
const money=n=>new Intl.NumberFormat('es-MX',{style:'currency',currency:'MXN'}).format(n);let stays=demo;
async function load(){try{const r=await fetch(A+'/v1/properties');if(r.ok){const j=await r.json();if(j.items?.length)stays=j.items}}catch{} render()}
function render(){document.querySelector('#app').innerHTML=`<header class="top"><div class="brand">LINOEM <b>HOTEL</b></div><button class="avatar" id="login">◎</button></header><main><div class="hero"><h1>Estancias que se sienten <em>extraordinarias.</em></h1><p>Hoteles, casas y departamentos seleccionados para ti.</p><form class="search" id="search"><div class="field"><label>DESTINO</label><input name="city" placeholder="¿A dónde vas?"></div><div class="field"><label>LLEGADA</label><input type="date" name="from"></div><div class="field"><label>SALIDA</label><input type="date" name="to"></div><button class="primary">Buscar</button></form></div><div class="content"><div class="rowtitle"><div><h2>Alojamientos destacados</h2><span>Disponibilidad actualizada en tiempo real</span></div></div><div class="grid">${stays.map(s=>`<article class="stay" data-id="${s.id}"><div class="photo">${s.icon||'✦'}</div><section><span class="badge">${s.type}</span><h3>${s.name}</h3><div class="meta">${s.city} · Hasta ${s.people||s.maxGuests} huéspedes</div><div class="price"><span>${money(s.price||s.basePrice)} / noche</span><span>★ 4.9</span></div></section></article>`).join('')}</div></div></main><nav class="nav"><button class="active">⌂<br>Explorar</button><button>♡<br>Favoritos</button><button>▣<br>Viajes</button><button>◎<br>Perfil</button></nav>`;document.querySelector('#login').onclick=login;document.querySelectorAll('.stay').forEach(x=>x.onclick=()=>reserve(x.dataset.id));document.querySelector('#search').onsubmit=e=>{e.preventDefault();const q=new FormData(e.target).get('city').toLowerCase();stays=demo.filter(x=>x.city.toLowerCase().includes(q)||x.name.toLowerCase().includes(q));render()}}
function login(){modal('Iniciar sesión',`<input placeholder="Correo electrónico"><input type="password" placeholder="Contraseña"><button class="primary" style="width:100%;padding:14px">Continuar</button><p class="meta">También puedes configurar acceso con Google en producción.</p>`)}
function reserve(id){const s=stays.find(x=>String(x.id)===id)||demo[0];modal('Reservar '+s.name,`<p>Selecciona tus fechas y confirma de forma segura.</p><input type="date"><input type="date"><button class="primary" style="width:100%;padding:14px">Continuar al pago</button><p class="meta">Mercado Pago se activa con tus credenciales sandbox.</p>`)}
function modal(title,body){const m=document.createElement('div');m.className='modal';m.innerHTML=`<div class="sheet"><button class="close">×</button><h2>${title}</h2>${body}</div>`;m.querySelector('.close').onclick=()=>m.remove();m.onclick=e=>{if(e.target===m)m.remove()};document.body.append(m)}load();
