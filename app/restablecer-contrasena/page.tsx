"use client";import {useEffect,useState} from "react";export default function Page(){const[m,setM]=useState("");const[token,setToken]=useState<string|null>(null);useEffect(()=>setToken(new URLSearchParams(location.search).get("token")),[]);async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const password=new FormData(e.currentTarget).get("password"),r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,password})}),j=await r.json();if(r.ok)setTimeout(()=>location.href="/login",1200);setM(r.ok?"Contraseña actualizada. Redirigiendo…":j.error)}return <main className="auth-card"><form className="form" onSubmit={go}><div className="brand">LINOEM HOTEL</div><h2>Nueva contraseña</h2>{m&&<div className="msg">{m}</div>}<label>Contraseña nueva</label><input name="password" type="password" minLength={10} required/><button className="btn">Guardar contraseña</button></form></main>}
