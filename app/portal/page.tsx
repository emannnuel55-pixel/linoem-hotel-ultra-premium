import {redirect} from "next/navigation";import {currentUser} from "@/lib/auth";import {db} from "@/lib/db";export default async function Portal(){const u=await currentUser();if(!u)redirect("/login");const bookings=await db.booking.findMany({where:{userId:u.id},include:{room:true},orderBy:{checkIn:"desc"}});return <><nav className="nav"><div className="brand">LINOEM HOTEL</div><form action="/api/auth/logout" method="post"><button className="btn outline">Cerrar sesión</button></form></nav><main className="portal"><p className="gold">PORTAL DEL HUÉSPED</p><h1>Hola, {u.name}</h1><div className="stats"><div className="stat"><strong>{bookings.length}</strong><p>Reservaciones</p></div><div className="stat"><strong>{bookings.filter(b=>b.status==="CONFIRMED").length}</strong><p>Confirmadas</p></div><div className="stat"><strong>24/7</strong><p>Atención al huésped</p></div></div><h2>Mis reservaciones</h2>{bookings.length?bookings.map(b=><div className="booking" key={b.id}><b>{b.room.name} · Habitación {b.room.number}</b><p>{b.checkIn.toLocaleDateString("es-MX")} — {b.checkOut.toLocaleDateString("es-MX")} · {b.status}</p></div>):<div className="booking">Aún no tienes reservaciones. El catálogo y el flujo de reservación están listos para la siguiente fase.</div>}</main></>}
