"use client";import {useState} from "react";export default function Page(){const[m,setM]=useState("");async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const email=new FormData(e.currentTarget).get("email");const r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});setM((await r.json()).message||"Revisa tu correo.")}return <main className="auth-card"><form className="form" onSubmit={go}><div className="brand">LINOEM HOTEL</div><h2>Recuperar acceso</h2><p>Te enviaremos un enlace seguro que vence en una hora.</p>{m&&<div className="msg">{m}</div>}<label>Correo electrónico</label><input name="email" type="email" required/><button className="btn">Enviar enlace</button><div className="links"><a href="/login">Volver al inicio</a></div></form></main>}
