"use client";import {useEffect,useState} from "react";export default function Login(){const[m,setM]=useState("");const[v,setV]=useState(false);useEffect(()=>setV(new URLSearchParams(location.search).get("verified")==="1"),[]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(f))}),j=await r.json();if(r.ok)location.href="/portal";else setM(j.error)}return <main className="auth"><section className="auth-art"><div className="brand">LINOEM HOTEL</div><h1>Descansa.<br/><span className="gold">Nosotros cuidamos el resto.</span></h1></section><section className="auth-card"><form className="form" onSubmit={submit}><h2>Bienvenido</h2><p>Ingresa para administrar tu estancia.</p>{v&&<div className="msg">Correo verificado. Ya puedes iniciar sesión.</div>}{m&&<div className="msg">{m}</div>}<label>Correo electrónico</label><input name="email" type="email" required/><label>Contraseña</label><input name="password" type="password" required/><button className="btn">Iniciar sesión</button><a className="btn google" href="/api/auth/google">Continuar con Google</a><div className="links"><a href="/registro">Crear cuenta</a><a href="/olvide-contrasena">Olvidé mi contraseña</a></div></form></section></main>}
